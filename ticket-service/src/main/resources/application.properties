spring.application.name=ms-tickets

# Import remote configuration from the Spring Cloud Config Server.
# "optional:" means the service can still start when the config-server is down.
spring.config.import=optional:configserver:${CONFIG_SERVER_URL:http://localhost:8889}
